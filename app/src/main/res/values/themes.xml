<resources>
    <!-- Base application theme. -->
    <style name="Theme.TestTask" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/background</item>
        <item name="android:itemTextAppearance">@style/Theme.TestTask.MenuTextAppearance</item>
    </style>

    <style name="Theme.TestTask.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="textView" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.TestTask.MenuTextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.TestTask.ItemTheme">
        <item name="android:background">@color/gray</item>
    </style>

    <style name="button" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">30sp</item>
        <item name="fontFamily">@font/roboto</item>
        <item name="textAllCaps">false</item>
        <item name="android:paddingStart">30dp</item>
        <item name="android:paddingEnd">30dp</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="Theme.TestTask.SqueezedButton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">30sp</item>
        <item name="fontFamily">@font/roboto</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>